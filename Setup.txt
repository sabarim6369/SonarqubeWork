✅ Step-by-Step: Run SonarQube Using Docker
This runs SonarQube locally at http://localhost:9000

🔹 1. Prerequisites
Install Docker

At least 2 GB RAM for Docker (recommended 4 GB)

🔹 2. Run SonarQube Container
bash
Copy
Edit
docker run -d --name sonarqube \
  -p 9000:9000 \
  -e SONAR_ES_BOOTSTRAP_CHECKS_DISABLE=true \
  sonarqube:latest
-d: Run container in detached mode

--name sonarqube: Name the container

-p 9000:9000: Map SonarQube port to your localhost

SONAR_ES_BOOTSTRAP_CHECKS_DISABLE=true: Allows SonarQube to run without strict Elasticsearch requirements (use only for dev)

🔹 3. Open SonarQube Dashboard
Once the container is up, open:

arduino
Copy
Edit
http://localhost:9000
Default credentials:

pgsql
Copy
Edit
Username: admin
Password: admin
🔹 4. Create Project & Token
Click on "Create Project"

Give a name & project key

Choose "Manually"

Generate a token — you'll use this in the next steps

✅ Example: Scan a Node.js/React/Java Project
Create a file in your project root:
sonar-project.properties

properties
Copy
Edit
sonar.projectKey=myproject
sonar.projectName=My Project
sonar.sources=src
sonar.host.url=http://localhost:9000
sonar.login=<your-generated-token>
Run Scanner:
bash
Copy
Edit
npx sonar-scanner
🔄 To Stop SonarQube
bash
Copy
Edit
docker stop sonarqube
docker rm sonarqube
